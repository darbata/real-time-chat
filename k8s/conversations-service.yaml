apiVersion: v1
kind: Service
metadata:
  name: conversations
  labels:
    app: conversations
  annotations:
    cloud.google.com/backend-config: '{"default": "backend-config"}'
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: conversations
  type: ClusterIP
